name: Cross-compat

on:
  push:
  pull_request:

jobs:
  adaptive-unit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - name: Run adaptive characterization tests
        run: npm run test:adaptive

  e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - name: Install Playwright (with WebKit)
        run: npx playwright install --with-deps
      - name: Run e2e (Chromium, Firefox, WebKit)
        run: npm run test:e2e -- --project=chromium --project=firefox --project=webkit
      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report
          if-no-files-found: ignore

  css-compat:
    runs-on: ubuntu-latest
    needs: e2e
    env:
      NEXT_PUBLIC_API_URL: https://api.blazekeyapp.com
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run build
      - name: CSS support scan (informational baseline)
        continue-on-error: true
        run: npm run compat:css


